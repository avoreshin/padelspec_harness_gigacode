<!--
  ОБРАЗЕЦ отчёта /java-linter-review. Субагент java-lint-review ОБЯЗАН
  придерживаться ИМЕННО этой структуры (эталон: java-linter-2026-07-17.md).
  Плейсхолдеры <...> заполняются реальными данными из SARIF/CPD-вывода.

  Инварианты структуры (менять нельзя):
    1. Заголовок «# Code Smell Report — <project>» + шапка (Дата / Всего файлов /
       Всего code smells).
    2. Секция «## По файлам» — группировка ПО ФАЙЛАМ (не по severity!). Для
       каждого файла: «Строк в классе» + нумерованный список smells в формате
       «N. [Severity] **Название** (строка K) — описание + рекомендация».
    3. «## Топ-5 категорий code smells» — таблица.
    4. «## Детальные заметки по критичным smells».
    5. «## Итого» — счётчики High/Medium/Low + ключевые рекомендации.

  Три обязательных элемента на каждую находку сохраняются: критичность
  ([High|Medium|Low]), кусок кода (файл + строка), рекомендация (в описании
  smell или в блоке ключевых рекомендаций).

  ОБЯЗАТЕЛЬНЫЕ проверки (должны присутствовать всегда, даже с нулём находок):
    • Неиспользуемые переменные / методы / импорты (dead code).
    • Нарушения SOLID (в первую очередь SRP — God-класс, слишком много
      ответственностей/полей/параметров) — отражаются в «Проверке SOLID».
-->
# Code Smell Report — <project-name>

**Дата:** <YYYY-MM-DD>
**Всего файлов:** <M main> main + <T test> test = <N> (проанализировано: <A> значимых файлов)
**Всего code smells:** <TOTAL>

---

## По файлам

<!-- Повторяющийся блок на каждый файл со smells. Файлы — по убыванию
     суммарной критичности. Внутри файла smells нумеруются; каждый:
     [Severity] **Название** (строка K) — описание проблемы + предметная
     рекомендация «что и как исправить». -->

### `<src/main/java/.../SomeClass.java>`
- **Строк в классе:** <LOC>
- **Smells:**
  1. [<High|Medium|Low>] **<Короткое название>** (строка <K>) — <описание, почему это smell, и что сделать>.
  2. [<...>] **<...>** (строка <...>) — <...>.

### `<src/main/java/.../AnotherClass.java>`
- **Строк в классе:** <LOC>
- **Smells:**
  1. [<...>] **<...>** (строка <...>) — <...>.

---

## Топ-5 категорий code smells

| # | Категория | Количество |
|---|-----------|:----------:|
| 1 | **<напр. Дублирование кода>** | <n> |
| 2 | **<напр. Длинные методы (>25 строк)>** | <n> |
| 3 | **<напр. Неиспользуемые поля/методы/импорты>** | <n> |
| 4 | **<напр. God-класс / нарушение SRP (SOLID)>** | <n> |
| 5 | **<напр. Сложная вложенность>** | <n> |

---

## Детальные заметки по критичным smells

<!-- Развёрнуто по каждой значимой категории: перечисление конкретных
     случаев с file:line и рекомендацией по рефакторингу. -->

### 1. <Категория> (<n> случаев)
- <case: `Class.method()` — что не так → как исправить>

### 2. <Категория> (<n> случаев)
- <...>

### Обязательно: Неиспользуемые переменные / методы / импорты
<!-- Всегда присутствует. Если ничего не найдено — явно написать
     «Не обнаружено». Иначе — список с file:line и действием REMOVE. -->
- <ClassX.fieldY — объявлено, не используется → удалить>
- <либо> Не обнаружено.

### Обязательно: Проверка SOLID
<!-- Всегда присутствует. Оценка по каждому принципу на основе структурных
     сигналов линтеров (GodClass, TooManyMethods/Fields, ExcessiveParameterList,
     CouplingBetweenObjects) + ревью субагента. Если нарушений нет — написать
     «Существенных нарушений не выявлено». -->
- **SRP (Single Responsibility):** <напр. `CacheService` — God-класс: матчинг + управление кэшами + валидация → выделить `MatchingService` и `CacheManager`>.
- **OCP (Open/Closed):** <оценка или «нарушений не выявлено»>.
- **LSP (Liskov):** <оценка или «нарушений не выявлено»>.
- **ISP (Interface Segregation):** <оценка или «нарушений не выявлено»>.
- **DIP (Dependency Inversion):** <оценка или «нарушений не выявлено»>.

---

## Итого

- **Высокий приоритет (High):** <a>
- **Средний приоритет (Medium):** <b>
- **Низкий приоритет (Low):** <c>
- **Итого:** <TOTAL> code smells

### Ключевые рекомендации (в порядке приоритета)
1. **<FIX BUG|REFACTOR|REMOVE>**: <конкретное действие>.
2. **<...>**: <...>.

---

## Приложение

- **Линтеры:** PMD `<✓|✗>`, Checkstyle `<✓|✗>`, PMD-CPD `<✓|✗>`, SpotBugs `<✓|⚠ skipped: reason>`
- **Версии:** см. `.gigacode/vendor/java-linters/versions.lock`
- **Rulesets:** `pmd-smells.xml`, `checkstyle-smells.xml`, CPD `--minimum-tokens <N>`
- **Источники (src roots):** <список src/main, src/test>
- **Целостность линтеров (SHA-256):** <ok | FAIL | skipped>
- **Сырые результаты:** `linter-reports/.tmp/*.sarif`, `*.xml` (удаляются после прогона, если не задан --keep)
