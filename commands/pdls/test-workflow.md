---
description: Оркестратор цикла тестирования — ведёт релиз через фазы profile → model → autotests → review → run → export, пишет переходы в audit-трейл и собирает свод по релизу. Отдельные команды при этом продолжают работать сами по себе
---

Проведи релиз через цикл тестирования: {{args}} (ключ релиза `ASFMSTD-NNNN`; флаг `--resume` — продолжить с последней записанной фазы).

**Тип задачи:** цикл тестировщика. **R1** до фазы `run`, **R2** дальше: прогон трогает общий стенд, а выгрузка готовит файл для внешней системы.

```
<EMIT>      ${extensionPath}/scripts/emit-phase-event.sh --loop testing
<STATE>     ${extensionPath}/scripts/workflow-state.sh
<EVIDENCE>  ${extensionPath}/scripts/collect-test-evidence.py
<COVERAGE>  ${extensionPath}/scripts/sdd-testcoverage.py
<READY>     ${extensionPath}/scripts/release-readiness.py
```

## Это не dev-цикл под другим именем

| | dev-цикл | цикл тестирования |
|---|---|---|
| Acceptance criteria | **SDD в `docs/sdd/`** | **тот же SDD**; тест-модель — его развёртка в проверяемые сценарии |
| Продукт фазы | код | сценарии в feature-файлах |
| «Тесты неприкосновенны» | да, гейт `test-files-protector` | **неприменимо** — тесты и есть продукт |
| Ревью | код против SDD | сценарии против тест-модели |
| Доказательство | evidence bundle | свод по релизу (`<release>.evidence.json`) |

Поэтому у него свой словарь фаз, и переходы пишутся с `--loop testing`. Событие dev-цикла остаётся ровно таким, каким было, — иначе всё, что уже читает трейл, читало бы новый формат.

## Acceptance criteria берутся из SDD

Раньше тест-модель была сама себе критерием: её порождал агент из описаний в трекере, и «человек потом поправит» было единственной гарантией. Цикл честно реализовывал модель, которая могла быть неверной, и ни один гейт этого не видел.

В полном цикле критерии — те же артефакты, что у разработки: `docs/sdd/*.md`, секция `## 4. Acceptance criteria` с блоками `AC-N` в Given-When-Then. Связь со story — строка `**Story**` в Metadata-таблице SDD. Читает их тот же `extract-sdd-frontmatter.sh`, которым SDD валидируются в `validate-schemas.sh` и в `sdd-schema-gate`: второй парсер разошёлся бы с первым молча.

| Проверка | Уровень |
|---|---|
| у story релиза нет SDD | Blocker — критерии не зафиксированы |
| SDD в статусе `draft`/`deprecated` | Blocker — документ ещё меняется, модель по нему устареет молча |
| на story два approved-SDD | Blocker — какой набор критериев полный, неясно |
| `AC-*` не покрыт ни одним кейсом | Blocker — критерий записан, а проверять его нечем |
| кейс ссылается на несуществующий `AC-*` | Blocker |
| у кейса нет `ac:` | Major |

Гейт `sdd-coverage-gate.sh` пересчитывает это сам и на `BLOCK` отдаёт `exit 2` в двух точках: выход из фазы `model` со статусом `pass` и переход в `done`.

## Барьер перед прогоном

Разработка ведётся в **другом репозитории**, другими людьми и в других сессиях: вести её цикл обвязка не может. Что она может — сверить состояние по общему артефакту.

| Статус SDD | Что разрешает |
|---|---|
| `approved` | `model` → `autotests` → `review` |
| `implemented` | плюс **прогон**: dev-цикл по story закрыт |

Фазы до прогона выводятся из критериев, а не из кода, поэтому идут параллельно разработке и ждать её не обязаны. Прогон — единственная фаза, которой нужен собранный продукт. Барьер потому один и стоит перед `run`; держит его тот же `sdd-coverage-gate`, третьей точкой срабатывания.

Evidence bundle разработки при этом не читается: он лежит по одному пути на репозиторий, перезаписывается каждой задачей и обычно не под git. `implemented` ставит человек после фазы evidence, и живёт этот признак в самом SDD.

Чего обвязка не наблюдает вовсе — **какая сборка стоит на стенде**. Даже при `READY` подтверждает это человек, и подтверждение идёт в свод как факт, а не как допущение.

Где искать SDD — строки `dev_repo` и `sdd_dir` в `docs/test-project-profile.md`. Не заполнены — в раздельном раскладе каждая story окажется без критериев.

**Только полный цикл.** Гейт висит на событии фазы, а не на командах: `/pdls:generate-test-cases`, `/pdls:generate-autotests` и `/pdls:review-autotests`, запущенные руками, фазовых событий не эмитят вовсе и потому ведут себя ровно как раньше — без единой новой проверки и без требования SDD.

Снять осознанно: `/pdls:harness hooks disable sdd-coverage-gate` (**R2**, обоснование — в свод по релизу).

## Фазы

| Фаза | Команда | Что фиксируется |
|---|---|---|
| `profile` | `/pdls:test-project` | чем написан проект, где тесты, чем запускается |
| `model` | `/pdls:generate-test-cases <release>` | тест-модель, развёрнутая по `AC-*` из SDD |
| `autotests` | `/pdls:generate-autotests <release>` | сценарии в feature-файлах |
| `review` | `/pdls:review-autotests <release> --fix` | вердикт сверки с тест-моделью |
| — | `/pdls:release-readiness <release>` | **барьер**: закрыта ли разработка по каждой story |
| `run` | `/pdls:test-run <release>` | прогон по тегам `@gen-*` |
| `export` | `/pdls:export-testcases-xml <release>` | XML для загрузки человеком |

## Процесс

1. Разобрать ключ релиза. Пусто — спросить.
2. `bash <STATE> <release>` — есть ли уже состояние. Есть и задан `--resume` — продолжить с `next_recommended_command`; есть без `--resume` — показать состояние и спросить, продолжать или начать заново.
3. Напечатать план и **спросить подтверждение** (`AskUserQuestion`) до первого шага.
4. Для каждой фазы: вызвать её команду, дождаться результата, записать переход:

```bash
bash <EMIT> <release> <from> <to> <status> <iteration> "<причина>"
```

   `status`: `pass` — фаза дала результат; `iterate` — повтор той же фазы; `fail` — откат на предыдущую; `escalate` — нужен человек.

5. **Между фазами не продолжать молча.** До `run` — авто-переход при `pass`, дальше (R2) — подтверждение на каждом шаге.
5a. Перед выходом из фазы `model` — `python3 <COVERAGE> <release> --project .`. Вердикт `BLOCK` переход не пропустит; разбирать его надо здесь, а не после генерации сценариев.
5b. Перед фазой `run` — `python3 <READY> <release> --project .`. `NOT READY` означает, что ждём чужой цикл, а не что что-то сломано: сценарии к этому моменту уже написаны. При `READY` **спросить у человека**, та ли сборка на стенде, — этого обвязка не видит.
6. После `export` — собрать свод: `python3 <EVIDENCE> <release> --run-status <...> --export-file <...>` и **затем** записать переход `export → done`. Порядок не косметический: цикл закрывает свод, а не запись `done`. Stop-гейт `test-loop-enforcer` требует свод у любой задачи тестового цикла в трейле, включая доведённую до `done`, — запись о завершении делает тот же агент, который свод и должен был собрать.

## Откаты

Возврат не в «начало», а туда, где лежит причина:

| Что случилось | Куда | Почему |
|---|---|---|
| ревью: `channel-drift`, `polarity-drift`, `no-assertion` | `autotests` | форма сценария, чинится генерацией и `--fix` |
| ревью: `step-uncovered`, ожидание сформулировано общо | `model` | правит человек: это про содержание проверки |
| покрытие: `ac-uncovered`, `ac-unknown` | `model` | кейса на критерий нет либо ссылка неверна |
| покрытие: `story-no-sdd`, `sdd-not-approved` | **останов, `escalate`** | критериев не существует — их пишет и утверждает человек, обвязка их не выдумывает |
| барьер: story не в `implemented` | ожидание, не откат | разработка в другом репозитории ещё идёт; сценарии уже готовы |
| ревью: `scenario-missing` | `autotests` | сценарий не был записан |
| прогон: `fix` | `autotests` | шаг не сматчился |
| прогон: `out-of-scope` | **останов, `escalate`** | дефект продукта либо неверное ожидание; подгонять сценарий запрещено |
| прогон: `infra` | останов | прогона не было, чинить окружение |

Потолок — **3 итерации** на фазу. Исчерпан — `escalate` и останов с журналом.

## Отдельные команды продолжают работать

Оркестратор — надстройка. Переходы пишет он, а не команды: `/pdls:generate-autotests`, вызванная руками, ведёт себя ровно как раньше — ничего не пишет в трейл и ничего не требует. Гейт `autotest-review-gate` работает одинаково в обоих режимах: он пересчитывает сверку по файлам, а не смотрит на состояние цикла.

## Output (в чат)

```
## Test Workflow — ASFMSTD-8441
  Фазы:    profile → model → autotests → review → run → export
  Режим:   авто до run, подтверждение дальше
  Потолок: 3 итерации на фазу

✅ profile    pass   cucumber-jvm / maven, инвентарь 312 шагов
✅ model      pass   11 кейсов, 3 отброшено · AC 11/11 по 4 SDD
✅ autotests  pass   9 сценариев в 2 файлах
🔁 review     iterate  BLOCK → починено 5 → REQUEST CHANGES
✅ review     pass   остались 2 major — в своде
⏸️ run        escalate  out-of-scope: expected APPROVED but was DECLINED

⏹️ Останов на фазе run. Сценарий доехал до проверки, сообщение ушло —
   дальше дефект продукта либо ожидание в тест-модели. Не зона обвязки.
   Состояние: bash ${extensionPath}/scripts/workflow-state.sh ASFMSTD-8441
   Продолжить: /pdls:test-workflow ASFMSTD-8441 --resume
```

## Next step

Цикл закрыт сводом `<release>.evidence.json`. **Коммит команда не делает** — ни тест-модель, ни сценарии, ни свод.
